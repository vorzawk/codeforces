#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define pb push_back
#define present(container, element) (container.find(element) != container.end()) 

string findThird(string c1, string c2, k) {
    l = vector<String> l;
    for(int i=0; i<k; i++) {
        if c1[i] == c2[i] {
            l.pb(c1[i])
        } else {
            unordered_set<char> s = 
int main() {

ios_base::sync_with_stdio(false);cin.tie(NULL);
int n;
cin >> n >> k;
unordered_set<string> deck;
ans = 0;


return 0;

}
